import styled from "@emotion/styled";
import { colors, typography } from "../../../constants/styling";

export const AnalyiticsTitle = styled.span`
  text-align: center;
  color: ${colors.textMuted};
  font-weight: ${typography.fontWeight.medium};
`;
